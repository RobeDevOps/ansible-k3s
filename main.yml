---
- hosts: masters:slaves
  gather_facts: yes
  become: yes
  roles:
    - etc_config
    - container_features
    - apt_packages
    - dphys_swapfile

#   tasks:
#     - name: Reboot Pi
#       become: True
#       reboot: